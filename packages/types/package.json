{
  "name": "@vs-platform/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and interfaces for the video streaming platform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  },
  "files": [
    "dist"
  ]
}

